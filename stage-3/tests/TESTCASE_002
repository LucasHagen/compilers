x int;
y[10] static int;
_teste123[40000] static string;

int main(int x, const string yyy, string i1)
{
    x = 1 + 2 - - 4 * 3 ^ 4 / 10 * - 1 + 3;
    break;
    continue;
    input x;
    output x + 1, y + 2 , x + ? x ? z + 333 : 1000 + 101233, fim;
    return 10 + + 10 + 10;
}

testeee[123123123] int;


int xxxx(int x, const string yyy, string i1)
{
    x = 1 + 2 - - 4 * 3 ^ 4 / 10 * - 1 + 3;
}
