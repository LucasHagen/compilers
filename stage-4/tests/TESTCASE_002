x int;
y[10] static int;
_teste123[40000] static string;

int funcaoTeste() {}

int main(int x, const string yyy, string i1)
{
    static const int x <= 10;
    int y;
    static int ystatic;
    const int z;
    if(1) then
    {

    };
    x = 1 + 2 - - 4 * 3 ^ 4 / 10 * - 1 + 3;
    break;
    funcaoTeste(1, 2, 3, 4);
    continue;
    int  a;
    input x;
    float left;
    float right;
    left << 10;
    right >> 12+1+a ^10;
    output x + 1, y + 2 , x + ? x ? z + 333 : 1000 + 101233, fim;
    return 10 + + 10 + 10 + funcaoo();
}

testeee[123123123] int;


int xxxx(int x, const string yyy, string i1)
{
    x = 1 + 2 - - 4 * 3 ^ 4 / 10 * - 1 + 3;
}
